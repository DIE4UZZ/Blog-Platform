# ACCEPTANCE_login-register

## 验收结果
- 项目结构已初始化（Vue3 + Tailwind + Element Plus + Axios + Router）。
- 登录/注册页面完成并具备响应式布局。
- API 模块包含拦截器、错误提示与基础 loading 状态支持。

## 自测记录
- 未执行本地运行与构建测试（需手动执行）。

## 建议验证步骤
```bash
cd frontend
npm run dev
```
